- realised that PID one goes for the container if used Run instead of Exec.
- The shell inside the container must have PID one, instead of the Go process (the container) itself.
- it helps in sending sigterm or such signals directly to the app running inside the container, instead of letting the container/the go process handle it. 